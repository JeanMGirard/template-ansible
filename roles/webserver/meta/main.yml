galaxy_info:
  role_name: webserver
  author: your_name
  description: Installs and configures Nginx web server
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
    - name: EL
      versions:
        - "8"
        - "9"
  galaxy_tags:
    - web
    - nginx

dependencies:
  - role: common
